---
phase: 07-docker-ci-cd-and-scripts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - server/package.json
  - server/test/stripe-webhook.test.mjs
autonomous: true
requirements: [OPS-03]
must_haves:
  truths:
    - "`bun run check` runs the syntax/build gate under Bun and exits 0"
    - "`bun run test:brand` runs the brand check under Bun and exits 0"
    - "`bun run test:stripe` (server) runs the Stripe webhook test under Bun and exits 0"
    - "Each migrated script has a `:node` fallback that runs under Node"
  artifacts:
    - path: "package.json"
      provides: "Bun-default check + test:brand scripts with :node fallbacks"
      contains: "check:node"
    - path: "server/package.json"
      provides: "Bun-runnable test:stripe with :node fallback"
      contains: "test:stripe:node"
    - path: "server/test/stripe-webhook.test.mjs"
      provides: "Async Stripe test-header signing (Bun SubtleCrypto-compatible)"
      contains: "generateTestHeaderStringAsync"
  key_links:
    - from: "server/test/stripe-webhook.test.mjs"
      to: "Stripe SDK async crypto"
      via: "await generateTestHeaderStringAsync"
      pattern: "generateTestHeaderStringAsync"
---

<objective>
Make the `check`, `test:brand` (root), and `test:stripe` (server) scripts run under Bun (OPS-03), each with a `:node` fallback per the established convention. The `test:stripe` task requires a real code fix: the test's signature helper uses Stripe's synchronous `generateTestHeaderString`, which throws `SubtleCryptoProvider cannot be used in a synchronous context` under Bun — the same SubtleCrypto-async constraint Phase 5 solved for the webhook handler (constructEventAsync). Swap it to the async variant.

Purpose: Phase 7 success criterion 3 requires these three scripts to run to completion under Bun. Two are pure script wiring; `test:stripe` additionally needs the async-crypto migration to actually pass under Bun.

Output: Bun-default `check`/`test:brand`/`test:stripe` scripts with `:node` fallbacks, and a Bun-compatible Stripe test helper.

ALL acceptance commands below were dry-run on the dev machine during planning (Bun is the repo default) and confirmed: `bun run check` exits 0, `bun run test:brand` exits 0, and the async-fixed `bun test/stripe-webhook.test.mjs` reports `8 passed, 0 failed` (exit 0) both with the harness/server under Bun (`SERVER_RUNTIME=bun`) and with the default node-spawned server — and `client/bun.lock`/`server/bun.lock` pass `bun install --frozen-lockfile`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Script conventions (Bun default + :node fallback) — follow exactly
@CLAUDE.md

<interfaces>
<!-- Current scripts (the lines being changed). -->
Root package.json:
  "check": "node --check server/index.js && npm --prefix client run build"
  "test:brand": "node scripts/check-brand.mjs"
Server package.json:
  "test:stripe": "node test/stripe-webhook.test.mjs"

<!-- VERIFIED on this machine during planning -->
- `bun build server/index.js --target=bun --outfile=/dev/null` exits 0 (bundles 895 modules) → valid Bun-native syntax/resolution gate.
- `bun scripts/check-brand.mjs` exits 0 → test:brand is Bun-compatible as-is.
- `bun server/test/stripe-webhook.test.mjs` (unmodified) FAILS: "SubtleCryptoProvider cannot be used in a synchronous context" at generateTestHeaderString (Webhooks.js).
- After swapping line 64 to `await generateTestHeaderStringAsync`, `bun test/stripe-webhook.test.mjs` reports 8 passed / 0 failed (exit 0), AND `SERVER_RUNTIME=bun bun test/stripe-webhook.test.mjs` also reports 8 passed / 0 failed — so the production (bun-server) path passes too.
- Stripe SDK installed in server/node_modules exposes `stripe.webhooks.generateTestHeaderStringAsync` (async variant, same `{ payload, secret }` opts) — confirmed present.

<!-- The exact line to fix in server/test/stripe-webhook.test.mjs -->
Line 28: const stripe = new Stripe('sk_test_dummy_key_for_signing_only');
Line 62: async function postEvent(eventObj, { secret = WEBHOOK_SECRET } = {}) {   // already async
Line 64: const header = stripe.webhooks.generateTestHeaderString({ payload, secret });  // SYNC → must become await ...Async
The test spawns the real server; runtime is selected by env SERVER_RUNTIME (node|bun), BUN_PATH points at the bun binary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Async Stripe test-header signing (Bun SubtleCrypto fix)</name>
  <read_first>
    - server/test/stripe-webhook.test.mjs (full — the postEvent helper at lines 62-69 and Stripe init at line 28)
    - server/node_modules/stripe/esm/Webhooks.js (confirm generateTestHeaderStringAsync signature — it takes the same opts object)
  </read_first>
  <files>server/test/stripe-webhook.test.mjs</files>
  <action>
    In `server/test/stripe-webhook.test.mjs`, change the single synchronous signature call inside the already-`async` `postEvent` function (line ~64) from `const header = stripe.webhooks.generateTestHeaderString({ payload, secret });` to `const header = await stripe.webhooks.generateTestHeaderStringAsync({ payload, secret });`.

    This mirrors the Phase 5 decision (D-08/D-09) where `constructEventAsync` replaced `constructEvent` to use Stripe's SubtleCrypto async provider under Bun. `generateTestHeaderStringAsync` takes the identical `{ payload, secret }` opts and returns a Promise<string>; `postEvent` is already async so adding `await` is the only change. Do NOT change the `new Stripe(...)` construction, the test logic, the spawned-server env, or the runtime-selection logic. This single-line async swap makes the helper runtime-agnostic (verified to keep passing under Node, which is required for the `:node` fallback).
  </action>
  <acceptance_criteria>
    - `grep -q 'await stripe.webhooks.generateTestHeaderStringAsync' server/test/stripe-webhook.test.mjs` succeeds
    - `grep -c 'generateTestHeaderString(' server/test/stripe-webhook.test.mjs` returns `0` (no remaining synchronous call — note the `(` excludes the `Async` variant)
    - Running the test under Bun completes without the SubtleCrypto error: `cd server && bun test/stripe-webhook.test.mjs` exits 0 (verified during planning: 8 passed, 0 failed)
  </acceptance_criteria>
  <verify>
    <automated>cd server && grep -q 'await stripe.webhooks.generateTestHeaderStringAsync' test/stripe-webhook.test.mjs && ! grep -q 'generateTestHeaderString(' test/stripe-webhook.test.mjs && bun test/stripe-webhook.test.mjs && echo OK</automated>
  </verify>
  <done>The Stripe test helper signs with the async SubtleCrypto variant; `bun test/stripe-webhook.test.mjs` runs to completion (exit 0) with no synchronous-crypto error, and the change remains Node-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Bun-default check / test:brand / test:stripe scripts with :node fallbacks</name>
  <read_first>
    - package.json (root — current `check`, `test:brand`, and existing `:node` fallback patterns like build:node/start:node)
    - server/package.json (server — current `test:stripe`, `test:runtime-smoke`, and `:node` aliases)
    - CLAUDE.md Commands section (Bun-default + :node fallback convention)
  </read_first>
  <files>package.json, server/package.json</files>
  <action>
    Apply the Bun-default + `:node`-fallback pattern (same shape as the existing build/start scripts) to the three OPS-03 scripts.

    Root `package.json`:
    - `check`: replace the current value with a Bun-native gate: `bun build server/index.js --target=bun --outfile=/dev/null && bun --cwd=client run build`. Rationale (state in the SUMMARY): Bun has no `node --check` syntax-only equivalent (`bun --check` does not exist); `bun build ... --target=bun --outfile=/dev/null` parses + resolves the full server module graph (verified: exits 0, bundles 895 modules) and serves as the Bun syntax/resolution gate, while the client build is invoked through Bun. This is option (b) from the migration decision — a Bun parse/build check substituting for `node --check`.
    - `check:bun`: same value as `check` (mirrors the existing `:bun` alias convention).
    - `check:node`: the previous Node behavior — `node --check server/index.js && npm --prefix client run build`.
    - `test:brand`: change to `bun scripts/check-brand.mjs` (verified Bun-compatible, exits 0).
    - `test:brand:bun`: `bun scripts/check-brand.mjs`.
    - `test:brand:node`: `node scripts/check-brand.mjs` (prior behavior).

    Server `server/package.json`:
    - `test:stripe`: change to `SERVER_RUNTIME=bun bun test/stripe-webhook.test.mjs`. Set `SERVER_RUNTIME=bun` so the Bun script exercises the PRODUCTION runtime path (server spawned under Bun), not just a node-spawned server — verified during planning to pass (8/0). Passes after Task 1's async fix.
    - `test:stripe:bun`: `SERVER_RUNTIME=bun bun test/stripe-webhook.test.mjs`.
    - `test:stripe:node`: `node test/stripe-webhook.test.mjs` (prior behavior; leaves SERVER_RUNTIME unset so the harness and server both run under Node; still works because Task 1 stays Node-compatible).

    Do not touch other scripts. Preserve JSON formatting/ordering style already used in each file.
  </action>
  <acceptance_criteria>
    - Root `check` runs under Bun: `bun run check` exits 0
    - Root `test:brand` runs under Bun: `bun run test:brand` exits 0
    - `node -e "const p=require('./package.json'); process.exit(p.scripts['check:node'] && p.scripts['test:brand:node'] ? 0 : 1)"` exits 0 (both root :node fallbacks present)
    - Server `test:stripe` runs under Bun: `cd server && bun run test:stripe` exits 0
    - `grep -q 'SERVER_RUNTIME=bun' server/package.json` succeeds (Bun test:stripe exercises the bun-server path)
    - `node -e "const p=require('./server/package.json'); process.exit(p.scripts['test:stripe:node']?0:1)"` exits 0 (server :node fallback present)
    - `grep -q 'node --check server/index.js' package.json` succeeds (Node syntax gate retained in check:node)
  </acceptance_criteria>
  <verify>
    <automated>bun run check && bun run test:brand && node -e "const p=require('./package.json'); process.exit(p.scripts['check:node']&&p.scripts['test:brand:node']?0:1)" && (cd server && bun run test:stripe) && node -e "const p=require('./server/package.json'); process.exit(p.scripts['test:stripe:node']?0:1)" && echo OK</automated>
  </verify>
  <done>`bun run check`, `bun run test:brand`, and (server) `bun run test:stripe` all exit 0; test:stripe runs the server under Bun (SERVER_RUNTIME=bun); each has a `:node` fallback alias; the Node `node --check` gate is preserved under `check:node`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → Stripe signature crypto | Test generates webhook signatures the server must verify; must use a real crypto provider |
| CI scripts → build/test execution | check/test scripts gate what is allowed to ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Spoofing | Stripe webhook signature verification path (exercised by test:stripe) | mitigate | Test signs with the async SubtleCrypto provider so the server's `constructEventAsync` verification is genuinely exercised under Bun (not bypassed); the negative/invalid-signature case in the existing test still asserts rejection (verified: "bad signature returns 400", "forged event granted nothing" pass) |
| T-07-05 | Tampering | check gate strength | accept | `bun build --target=bun` parses+resolves the full module graph; weaker than runtime tests but matches the prior `node --check` scope (syntax/resolution gate, not behavior) |
| T-07-SC | Tampering | npm/bun installs | mitigate | No new packages added; scripts only re-point existing test entrypoints. No install step in this plan |
</threat_model>

<verification>
- All three scripts are LOCALLY verifiable under Bun on this dev machine (Bun is the repo default); acceptance commands run them directly and assert exit 0. All three were dry-run during planning and confirmed exit 0.
- `:node` fallbacks asserted by presence + (verified) Node-compatibility of the async test fix.
- No Docker / VPS dependency in this plan → fully autonomous.
</verification>

<success_criteria>
- `bun run check`, `bun run test:brand`, and server `bun run test:stripe` exit 0.
- `check:node`, `test:brand:node`, and `test:stripe:node` fallbacks exist and run under Node.
- The Stripe test helper uses `generateTestHeaderStringAsync` (Bun SubtleCrypto-compatible, Node-compatible).
- `test:stripe` exercises the production bun-server path (SERVER_RUNTIME=bun).
- The `node --check` syntax gate is retained in `check:node`.
</success_criteria>

<output>
Create `.planning/phases/07-docker-ci-cd-and-scripts/07-02-SUMMARY.md` when done.
</output>
