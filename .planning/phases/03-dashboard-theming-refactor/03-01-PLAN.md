---
phase: 03-dashboard-theming-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/tailwind.config.cjs
  - client/src/tailwind.css
  - client/src/utils/brandTheme.js
  - client/src/context/branding.jsx
autonomous: true
requirements: [THEME-01, THEME-03, THEME-04]
must_haves:
  truths:
    - "A company with no custom palette renders the exact Material Design green currently shown (pixel-identical fallback)"
    - "Logging in as a company with a custom palette re-themes brand chrome without a page reload"
    - "Logout or company switch resets brand variables to the green defaults"
    - "Brand palette is never written to localStorage"
  artifacts:
    - path: "client/tailwind.config.cjs"
      provides: "brand color family resolving to --brand-* CSS variables via rgb(var() / <alpha-value>)"
      contains: "rgb(var(--brand-500) / <alpha-value>)"
    - path: "client/src/tailwind.css"
      provides: ":root brand defaults equal to Material Design green channel triplets"
      contains: "--brand-500: 76 175 80"
    - path: "client/src/utils/brandTheme.js"
      provides: "pure palette->shade-scale derivation, hex->channel conversion, frozen DEFAULT_BRAND_SCALE"
      exports: ["DEFAULT_BRAND_SCALE"]
    - path: "client/src/context/branding.jsx"
      provides: "variable-writer effect writing/removing --brand-* on document.documentElement"
      contains: "setProperty"
  key_links:
    - from: "client/src/context/branding.jsx"
      to: "document.documentElement"
      via: "style.setProperty / style.removeProperty in useEffect keyed on branding"
      pattern: "documentElement\\.style\\.(setProperty|removeProperty)"
    - from: "client/src/context/branding.jsx"
      to: "client/src/utils/brandTheme.js"
      via: "import of derivation function"
      pattern: "from \"@/utils/brandTheme\""
    - from: "client/tailwind.config.cjs"
      to: "client/src/tailwind.css"
      via: "brand-* utilities resolve to :root --brand-* defaults"
      pattern: "--brand-"
---

<objective>
Build the runtime CSS-variable theming layer that all call-site migrations will consume: a Tailwind `brand` color family resolving to `--brand-*` variables, `:root` defaults equal to the exact Material Design green that `withMT` renders today, a pure shade-derivation utility, and a variable-writer effect in `BrandingProvider`.

Purpose: This is the mechanical foundation (THEME-01). The `:root` defaults guarantee a pixel-identical no-palette fallback (THEME-03), and the writer's reset behavior guarantees tenant isolation with no localStorage persistence (THEME-04). No call-site is touched here — that is Wave 2.

Output: A `brand-*` utility namespace usable across the app, a default green theme indistinguishable from pre-v3, and runtime re-theming on branding state change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-theming-refactor/03-CONTEXT.md
@.planning/phases/03-dashboard-theming-refactor/03-PATTERNS.md

<interfaces>
<!-- BrandingProvider state shape the writer consumes (branding.jsx lines 13-25, 93-107). -->
<!-- branding = palette(primary, accent, light, dark) + logo + isCustom + updatedAt -->
<!-- The provider ALREADY resets to EMPTY_BRANDING (isCustom: false) on logout/company change. -->
<!-- Palette is in-memory only; AuthContext owns localStorage for auth — palette is never persisted. -->

<!-- brandTheme.js conventions copied from utils/companyBranding.js: -->
<!-- frozen default export, named pure functions, HEX_COLOR regex, normalizeBrandHex reuse. -->

<!-- Material Design green channel triplets (the green withMT renders — verified in built CSS): -->
<!-- 50:232 245 233  100:200 230 201  200:165 214 167  300:129 199 132  400:102 187 106 -->
<!-- 500:76 175 80   600:67 160 71    700:56 142 60     800:46 125 50     900:27 94 32 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add brand color family to Tailwind config and :root defaults to tailwind.css</name>
  <files>client/tailwind.config.cjs, client/src/tailwind.css</files>
  <read_first>
    - client/tailwind.config.cjs (current withMT config — extend.colors is empty)
    - client/src/tailwind.css (existing :root --cb-* block, lines 9-18 — do NOT touch --cb-* vars)
    - .planning/phases/03-dashboard-theming-refactor/03-PATTERNS.md (Critical Findings 1 and 2; tailwind.config.cjs and tailwind.css pattern blocks)
  </read_first>
  <behavior>
    - After build, bg-brand-600 resolves to rgb(var(--brand-600) / ...) and renders rgb(67 160 71) against the default :root.
    - Opacity modifiers work: shadow-brand-500/20 is a valid class (family uses the alpha-value channel form).
    - With no inline override on documentElement, brand utilities render the exact Material Design green hexes (pixel-identical to current green-*).
  </behavior>
  <action>
    In client/tailwind.config.cjs, add a `brand` family under theme.extend.colors with shades 50,100,200,300,400,500,600,700,800,900, each set to the string "rgb(var(--brand-N) / <alpha-value>)" (channel form per Critical Finding 2 — plain hex would break every /N opacity modifier). `extend` deep-merges on top of withMT's replaced palette, so this is additive and does not disturb the `green` enum used elsewhere.
    In client/src/tailwind.css, add a NEW :root block (near the top, clearly commented as "Brand theme defaults — Material Design green; overridden at runtime by BrandingProvider") declaring --brand-50 through --brand-900 as space-separated RGB channel triplets equal to the Material Design green scale (50: 232 245 233, 100: 200 230 201, 200: 165 214 167, 300: 129 199 132, 400: 102 187 106, 500: 76 175 80, 600: 67 160 71, 700: 56 142 60, 800: 46 125 50, 900: 27 94 32). These hexes are what withMT renders for green-* (Critical Finding 1) — NOT Tailwind's #22c55e family. Leave the existing --cb-* variables untouched.
  </action>
  <verify>
    <automated>grep -q 'rgb(var(--brand-500) / <alpha-value>)' client/tailwind.config.cjs && grep -qE '^[[:space:]]*--brand-500:[[:space:]]*76 175 80' client/src/tailwind.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'rgb(var(--brand-' client/tailwind.config.cjs returns 10 (one per shade).
    - client/src/tailwind.css :root contains --brand-500: 76 175 80 and --brand-600: 67 160 71 exactly.
    - Existing --cb-* variables in tailwind.css are unchanged (grep -q -- '--cb-purple' client/src/tailwind.css still passes).
    - bun run build exits 0.
  </acceptance_criteria>
  <done>Tailwind brand-* utilities exist and resolve to MD-green-channel :root defaults; build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create brandTheme.js shade-derivation utility</name>
  <files>client/src/utils/brandTheme.js</files>
  <read_first>
    - client/src/utils/companyBranding.js (module conventions: frozen exports, HEX_COLOR regex, normalizeBrandHex — REUSE these patterns, do not duplicate validation)
    - .planning/phases/03-dashboard-theming-refactor/03-PATTERNS.md (brandTheme.js pattern block; "No Analog Found" note — interpolation algorithm is your discretion)
    - .planning/phases/03-dashboard-theming-refactor/03-CONTEXT.md (Claude's Discretion: mix ratios, anchor mapping)
  </read_first>
  <behavior>
    - hexToChannels("#4CAF50") returns the string "76 175 80".
    - DEFAULT_BRAND_SCALE has keys 50..900, each a channel triplet string, with 500 === "76 175 80" and 600 === "67 160 71" (single source of truth shared with the writer's reset; matches the CSS :root by design).
    - deriveBrandScale(palette) returns an object keyed 50..900 of channel triplet strings; primary anchors ~500/600, light anchors the 50 end, dark anchors the 900 end, accent informs hover steps.
    - Invalid/missing input falls back to DEFAULT_BRAND_SCALE (does not throw).
  </behavior>
  <action>
    Create client/src/utils/brandTheme.js following companyBranding.js conventions. Export: a hexToChannels(hex) pure function returning a space-separated "r g b" string (reuse normalizeBrandHex semantics for validation — return null/fallback on invalid hex); a frozen DEFAULT_BRAND_SCALE object keyed 50..900 whose values are the Material Design green channel triplets (500: "76 175 80", 600: "67 160 71", etc. — the same numbers as the CSS :root, deliberately duplicated so first paint is correct without JS); and deriveBrandScale(palette) taking the four-color palette (primary/accent/light/dark) and returning a 50..900 scale of channel triplet strings. Anchor primary near 500/600, interpolate toward light for the 50-end shades and toward dark for the 900-end shades, and use accent to inform the hover/active steps — exact mix ratios are your discretion. Guard against malformed palettes by returning DEFAULT_BRAND_SCALE. Pure functions only — no DOM, no React imports.
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./client/src/utils/brandTheme.js').then(m=>{const s=m.DEFAULT_BRAND_SCALE; if(s['500']!=='76 175 80'||s['600']!=='67 160 71')throw new Error('bad default scale'); if(m.hexToChannels('#4CAF50')!=='76 175 80')throw new Error('bad hexToChannels'); const d=m.deriveBrandScale({primary:'#2196F3',accent:'#1565C0',light:'#BBDEFB',dark:'#0D47A1'}); if(!d['500']||typeof d['500']!=='string')throw new Error('bad derive'); console.log('OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - Module imports without error in plain Node ESM (no React/DOM dependency).
    - DEFAULT_BRAND_SCALE['500'] === '76 175 80' and ['600'] === '67 160 71'.
    - hexToChannels('#4CAF50') === '76 175 80'.
    - deriveBrandScale returns an object with all keys 50..900 as channel triplet strings for a valid palette, and returns DEFAULT_BRAND_SCALE for empty/invalid input.
    - bun run build exits 0.
  </acceptance_criteria>
  <done>Pure brandTheme utility derives a full shade scale from a four-color palette and exposes the MD-green default scale.</done>
</task>

<task type="auto">
  <name>Task 3: Add variable-writer effect to BrandingProvider</name>
  <files>client/src/context/branding.jsx</files>
  <read_first>
    - client/src/context/branding.jsx (full file — state shape lines 13-25, existing fetch/reset effect lines 93-107; the writer is a NEW sibling effect following the same lifecycle)
    - client/src/utils/brandTheme.js (the deriveBrandScale + DEFAULT_BRAND_SCALE created in Task 2)
    - .planning/phases/03-dashboard-theming-refactor/03-PATTERNS.md (branding.jsx pattern block; Tenant isolation shared pattern)
  </read_first>
  <action>
    Import deriveBrandScale from @/utils/brandTheme (per import conventions, @ alias). Add a useEffect inside BrandingProvider keyed on branding. When branding.isCustom === true: compute the scale via deriveBrandScale(branding.palette) and write each shade with document.documentElement.style.setProperty("--brand-50", value) through --brand-900. When branding.isCustom === false (this already covers logout and company switch — the provider resets to EMPTY_BRANDING on companyId loss): call document.documentElement.style.removeProperty("--brand-50") through --brand-900 so the :root stylesheet defaults show through (removing, not re-writing defaults, guarantees byte-identical fallback CSS). Do NOT read or write localStorage — the palette stays in memory (THEME-04). Re-theming without reload (success criterion 1) falls out of the existing companyId-driven fetch flow, so no reload logic is added.
  </action>
  <verify>
    <automated>grep -qE 'documentElement\.style\.setProperty' client/src/context/branding.jsx && grep -qE 'documentElement\.style\.removeProperty' client/src/context/branding.jsx && grep -q 'from "@/utils/brandTheme"' client/src/context/branding.jsx && ! grep -niE 'localStorage' client/src/context/branding.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - branding.jsx imports deriveBrandScale from @/utils/brandTheme.
    - A useEffect calls documentElement.style.setProperty for --brand-50..--brand-900 when isCustom is true, and removeProperty for the same when isCustom is false.
    - No localStorage reference appears in branding.jsx (palette stays in memory — THEME-04).
    - bun run build exits 0.
  </acceptance_criteria>
  <done>BrandingProvider writes derived brand variables on custom palette, removes them (restoring :root defaults) on default/logout/company-switch, with no persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory branding state → DOM | Authenticated user's palette is written to document.documentElement; no untrusted external input crosses here |
| client → localStorage | Auth tokens already stored; palette explicitly must NOT be persisted (THEME-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | Cross-tenant palette leakage on shared machine | mitigate | Palette never written to localStorage; writer removes inline vars when isCustom=false (logout/company-switch resets to defaults) |
| T-03-02 | Tampering | Malformed palette injected into deriveBrandScale | mitigate | deriveBrandScale validates via normalizeBrandHex and falls back to DEFAULT_BRAND_SCALE rather than throwing or injecting raw values |
| T-03-SC | Tampering | npm/bun installs | accept | No new packages added in this phase; brandTheme.js is pure JS using existing patterns |
</threat_model>

<verification>
- bun run build exits 0 (vite build succeeds; brand-* classes compile).
- Default render is pixel-identical: with no custom palette, --brand-* fall back to MD-green :root defaults equal to what green-* renders today.
- Manual visual check: log in as a no-palette company → brand chrome looks identical to pre-v3 (Wave 2 sites not yet migrated, so verify the :root defaults via devtools computed --brand-500 = 76 175 80).
</verification>

<success_criteria>
- THEME-01: brand colour is driven by --brand-* CSS variables fed from BrandingProvider state.
- THEME-03: no-palette companies fall back to MD-green :root defaults (visually identical to current app).
- THEME-04: changing a palette only affects the in-memory tenant context; logout/company-switch removes inline vars; nothing persisted to localStorage.
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-theming-refactor/03-01-SUMMARY.md` when done.
</output>
