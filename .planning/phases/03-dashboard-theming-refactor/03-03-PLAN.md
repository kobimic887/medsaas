---
phase: 03-dashboard-theming-refactor
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - client/src/widgets/layout/dashboard-navbar.jsx
  - client/src/pages/main/blog.jsx
autonomous: true
requirements: [THEME-02]
must_haves:
  truths:
    - "Dashboard navbar cart totals/prices and the Send Enquiry CTA render in the company palette"
    - "Blog admin edit pencil and the Create Post CTA render in the company palette"
  artifacts:
    - path: "client/src/widgets/layout/dashboard-navbar.jsx"
      provides: "cart Total/price Typography and Send Enquiry Button migrated to brand-*"
      contains: "brand-"
    - path: "client/src/pages/main/blog.jsx"
      provides: "edit-post IconButton and Create Post gradient Button migrated to brand-*"
      contains: "brand-"
  key_links:
    - from: "navbar + blog brand call-sites"
      to: "tailwind brand family"
      via: "brand-N utility / className recipes resolving to --brand-N"
      pattern: "(text|bg|from|to|hover:bg|active:bg|shadow|hover:shadow)-brand-"
---

<objective>
Migrate the BRAND-classified green call-sites in the dashboard navbar (chrome) and the blog admin page onto the `brand-*` utilities from Plan 01, dropping the Material Tailwind `color="green"` enum props and replacing them with the equivalent brand className recipes.

Purpose: THEME-02 for the chrome + blog surface. These files have no overlap with Plan 02, so this plan runs in parallel within Wave 2.

Output: Navbar cart UI and CTAs, plus blog admin affordances, re-tint with the company palette.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-theming-refactor/03-CONTEXT.md
@.planning/phases/03-dashboard-theming-refactor/03-PATTERNS.md

<interfaces>
<!-- The brand-* Tailwind family already exists (Plan 01). Drop color="green" enum props, add full className. -->
<!-- MT class recipes (from 03-PATTERNS.md "Call-site files" table): -->
<!--   Typography color="green"      -> text-brand-500 -->
<!--   IconButton variant="text"     -> text-brand-500 hover:bg-brand-500/10 active:bg-brand-500/30 -->
<!--   Button (filled, default)      -> bg-brand-500 text-white shadow-md shadow-brand-500/20 hover:shadow-lg hover:shadow-brand-500/40 focus:opacity-[0.85] focus:shadow-none active:opacity-[0.85] active:shadow-none -->
<!--   Button variant="gradient"     -> bg-gradient-to-tr from-brand-600 to-brand-400 text-white shadow-md shadow-brand-500/20 hover:shadow-lg hover:shadow-brand-500/40 active:opacity-[0.85] -->
<!-- Dropping the enum prop -> MT falls back to default color; the className must fully override bg/text/shadow -->
<!-- (tailwind-merge lets the full className set win). Spot-check one Button and the IconButton visually. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate dashboard-navbar.jsx brand sites</name>
  <files>client/src/widgets/layout/dashboard-navbar.jsx</files>
  <read_first>
    - client/src/widgets/layout/dashboard-navbar.jsx (lines around 461, 484, 518)
    - .planning/phases/03-dashboard-theming-refactor/03-PATTERNS.md (Complete Call-Site Inventory MT props #12-14; swap recipes)
  </read_first>
  <action>
    Drop color="green" and apply brand className per recipe on all three (all brand-classified): line 461 Typography cart "Total: $" → text-brand-500; line 484 Typography cart item price → text-brand-500; line 518 Button "Send Enquiry" → filled-Button recipe (bg-brand-500 text-white shadow-md shadow-brand-500/20 hover:shadow-lg hover:shadow-brand-500/40 focus:opacity-[0.85] focus:shadow-none active:opacity-[0.85] active:shadow-none). The Button className must fully override the default-color background/text/shadow.
  </action>
  <verify>
    <automated>! grep -qE 'color="green"' client/src/widgets/layout/dashboard-navbar.jsx && grep -c 'text-brand-500' client/src/widgets/layout/dashboard-navbar.jsx | grep -qE '^[2-9]' && grep -q 'bg-brand-500' client/src/widgets/layout/dashboard-navbar.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Zero color="green" remaining in dashboard-navbar.jsx.
    - Two cart Typography sites use text-brand-500; the Send Enquiry Button uses the full filled brand recipe (bg-brand-500 + shadow-brand-500/20 etc.).
    - bun run build exits 0.
  </acceptance_criteria>
  <done>Navbar cart totals/prices and Send Enquiry CTA use brand-* and re-tint with the palette.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate blog.jsx brand sites</name>
  <files>client/src/pages/main/blog.jsx</files>
  <read_first>
    - client/src/pages/main/blog.jsx (lines around 241, 325)
    - .planning/phases/03-dashboard-theming-refactor/03-PATTERNS.md (Complete Call-Site Inventory MT props #3-4; swap recipes)
  </read_first>
  <action>
    Drop color="green" and apply brand className per recipe on both (both brand-classified): line 241 IconButton variant="text" edit-post pencil → text-brand-500 hover:bg-brand-500/10 active:bg-brand-500/30; line 325 Button variant="gradient" "Create Post" → gradient recipe (bg-gradient-to-tr from-brand-600 to-brand-400 text-white shadow-md shadow-brand-500/20 hover:shadow-lg hover:shadow-brand-500/40 active:opacity-[0.85]). Keep variant="text" / variant="gradient" props; only the color enum is dropped and replaced by className. The gradient Button className must fully override the default-color gradient.
  </action>
  <verify>
    <automated>! grep -qE 'color="green"' client/src/pages/main/blog.jsx && grep -q 'from-brand-600' client/src/pages/main/blog.jsx && grep -q 'hover:bg-brand-500/10' client/src/pages/main/blog.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Zero color="green" remaining in blog.jsx.
    - Edit-post IconButton uses text-brand-500 hover:bg-brand-500/10 active:bg-brand-500/30; Create Post Button uses from-brand-600 to-brand-400 gradient recipe.
    - bun run build exits 0.
  </acceptance_criteria>
  <done>Blog admin pencil and Create Post CTA use brand-* and re-tint with the palette.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Pure client-side className changes; no new input or data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Dropped enum prop leaving MT default color showing through | mitigate | className applies the full MT-rendered class set so tailwind-merge wins over the default-color variant; spot-check the Send Enquiry Button and Create Post Button visually |
| T-03-SC | Tampering | npm/bun installs | accept | No package installs in this plan |
</threat_model>

<verification>
- bun run build exits 0.
- grep: zero color="green" in both files; brand-* present per recipe.
- Manual: custom-palette company → navbar cart totals/Send Enquiry and blog pencil/Create Post re-tint; spot-check the dropped-enum Buttons render the full brand treatment (no MT default color bleed-through).
</verification>

<success_criteria>
- THEME-02: chrome + blog brand call-sites migrated to brand-*.
- Success criterion 4 holds for these migrated brand sites (no color="green" remaining; no green-/emerald- brand utilities).
</success_criteria>

<output>
Create `.planning/phases/03-dashboard-theming-refactor/03-03-SUMMARY.md` when done.
</output>
