---
phase: 02-branding-management
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - client/src/context/branding.jsx
  - client/src/hooks/useBranding.js
  - client/src/main.jsx
  - client/src/layouts/dashboard.jsx
  - client/src/widgets/layout/sidenav.jsx
autonomous: true
requirements: [LOGO-04, PALETTE-04]
user_setup: []

must_haves:
  truths:
    - "Every authenticated company user loads branding from the server through one shared provider"
    - "The dashboard sidebar displays the saved company logo plus company name without logout or page reload after refreshBranding"
    - "Companies without a logo retain the current company-name fallback with no broken image"
    - "Logo data and palettes remain tenant-specific because the client never selects a company ID"
    - "Auth-page company-name preview behavior remains backward-compatible"
  artifacts:
    - path: "client/src/context/branding.jsx"
      provides: "Shared authenticated branding state and refreshBranding API"
      min_lines: 70
    - path: "client/src/hooks/useBranding.js"
      provides: "Backward-compatible hook merging preview naming with shared server branding"
      contains: "refreshBranding"
    - path: "client/src/widgets/layout/sidenav.jsx"
      provides: "Logo plus company-name sidebar header with load-error fallback"
      contains: "brandImg"
  key_links:
    - from: "client/src/main.jsx"
      to: "client/src/context/branding.jsx"
      via: "BrandingProvider nested inside AuthProvider"
      pattern: "BrandingProvider"
    - from: "client/src/context/branding.jsx"
      to: "GET /api/company/branding"
      via: "authorized same-origin fetch"
      pattern: "company/branding"
    - from: "client/src/layouts/dashboard.jsx"
      to: "client/src/widgets/layout/sidenav.jsx"
      via: "brandImg and brandName props from useBranding"
      pattern: "brandImg"
---

<objective>
Create shared company-branding client state and wire the saved logo into the
dashboard sidebar for all users of the tenant.

Purpose: Fulfill the visible LOGO-04 result and give the later admin editor a
single `refreshBranding()` call that updates dashboard chrome immediately.

Output: `BrandingProvider`, extended `useBranding`, provider wiring, and resilient
sidebar logo rendering.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-branding-management/02-CONTEXT.md
@.planning/phases/02-branding-management/02-UI-SPEC.md
@.planning/phases/02-branding-management/02-PATTERNS.md
@.planning/phases/02-branding-management/02-01-PLAN.md
@client/src/context/auth.jsx
@client/src/hooks/useBranding.js
@client/src/main.jsx
@client/src/layouts/dashboard.jsx
@client/src/widgets/layout/sidenav.jsx

<interfaces>
`BrandingContext` value:
`{ branding, loading, error, refreshBranding }`

`branding` shape mirrors backend:
`{ palette, logo, isCustom, updatedAt }`

Extended `useBranding(previewCompanyName)` returns:
`{ brandName, companyName, platformName, palette, logo, brandingLoading, brandingError, refreshBranding }`
</interfaces>
</context>

## Artifacts this phase produces

- `BrandingProvider`
- `BrandingContext`
- `useBrandingContext`
- extended `useBranding`
- sidebar company logo rendering and fallback state

<tasks>

<task type="auto">
  <name>Task 1: Add authenticated shared branding state</name>
  <files>client/src/context/branding.jsx, client/src/hooks/useBranding.js, client/src/main.jsx</files>
  <read_first>
    - client/src/context/auth.jsx (provider and stored-user conventions)
    - client/src/hooks/useBranding.js (preserve previewCompanyName behavior)
    - client/src/config/branding.js (current preview/user/platform precedence, including uncommitted changes)
    - client/src/main.jsx (provider nesting)
    - client/src/utils/constants.js (getAuthToken and API_CONFIG)
    - server/index.js GET /api/company/branding response from Plan 01
  </read_first>
  <action>
    Create `BrandingProvider` inside AuthProvider. When an authenticated user has
    `companyId`, fetch `/api/company/branding` with the bearer token. Reset to a
    safe default when logged out/no company. Guard stale async responses during
    user/company changes. Expose an async `refreshBranding` that updates provider
    state and returns the loaded branding.

    Extend `useBranding(previewCompanyName)` rather than replacing it. Preserve
    the existing explicit preview-name precedence used by sign-up, and add the
    shared palette/logo/loading/error/refresh fields. Auth pages must not trigger
    a company branding request when no authenticated company exists.

    Wrap `BlogProvider`/`App` with `BrandingProvider` inside `AuthProvider` in
    `main.jsx`. Do not modify unrelated auth interception or Material Tailwind
    providers.
  </action>
  <acceptance_criteria>
    - One provider owns branding fetch state; individual dashboard components do not duplicate GET requests.
    - Fetch includes Authorization and uses `API_CONFIG.buildApiUrl`.
    - Provider resets branding when user/company disappears and prevents stale company responses from winning.
    - `useBranding(organization)` still returns the typed sign-up organization ahead of a stored user.
    - `refreshBranding` is callable by Company Admin in Plan 03.
  </acceptance_criteria>
  <verify>
    <automated>bun --cwd=client run build</automated>
  </verify>
  <done>Shared branding state loads once per company, remains auth-preview compatible, and exposes refreshBranding.</done>
</task>

<task type="auto">
  <name>Task 2: Render saved logo plus company name in sidebar chrome</name>
  <files>client/src/layouts/dashboard.jsx, client/src/widgets/layout/sidenav.jsx</files>
  <read_first>
    - client/src/layouts/dashboard.jsx (current useBranding and hardcoded brandImg)
    - client/src/widgets/layout/sidenav.jsx (header, brandName, PropTypes)
    - .planning/phases/02-branding-management/02-UI-SPEC.md sections Dashboard chrome result and Accessibility
    - client/src/context/branding.jsx from Task 1
  </read_first>
  <action>
    Read `logo` from `useBranding()` in Dashboard and pass its data URL as
    `brandImg`; remove the hardcoded `/img/logo-ct.png` assignment.

    Update Sidenav header to render the image only when a company logo exists,
    constrained to 160x48 with `object-contain`, followed by the company name.
    Track image load failure locally and fall back to text-only without a
    broken-image icon. Set alt text to `${brandName} logo`. Reset the error state
    when brandImg changes. Preserve mobile close behavior, route rendering, and
    existing active-button styling (Phase 3 owns color migration).
  </action>
  <acceptance_criteria>
    - No hardcoded `brandImg="/img/logo-ct.png"` remains in Dashboard.
    - Saved logo and company name render together in Sidenav.
    - Missing or failed image renders company-name text only.
    - Image uses object-contain and never crops/stretches.
    - Existing dashboard navigation behavior is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>bun --cwd=client run build && ! rg -n 'brandImg=\"/img/logo-ct.png\"' client/src/layouts/dashboard.jsx</automated>
  </verify>
  <done>All tenant users see their company's saved logo in sidebar chrome with a resilient text fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auth context -> branding fetch | Stored JWT/company identity controls which branding response is requested |
| Server branding response -> DOM image | Tenant-controlled logo data becomes a browser image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | client branding request | mitigate | Client sends no company ID; server derives tenant from JWT |
| T-02-07 | Tampering | stale async response after tenant/user change | mitigate | Cancel/ignore stale requests keyed to current companyId |
| T-02-08 | XSS | logo rendered in DOM | mitigate | Backend returns normalized PNG data only; render with `<img>`, never inject SVG/HTML |
</threat_model>

<verification>
- Client production build passes.
- Source assertions confirm one provider, authorized endpoint use, and no hardcoded Dashboard brand image.
- Manual behavior is covered by the final phase verification after Plan 03 can save branding.
</verification>

<success_criteria>
- LOGO-04 and client loading side of PALETTE-04 are implemented for all tenant users.
- Shared state can refresh immediately after an admin save.
</success_criteria>

<output>
Create `.planning/phases/02-branding-management/02-02-SUMMARY.md` when done.
</output>

