---
phase: 04-compatibility-spike-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/04-stripe.ts
  - spike/05-rdkit.ts
  - spike/STRIPE-HANDOFF.md
autonomous: true
requirements: [CMPT-04, CMPT-05]
must_haves:
  truths:
    - "Stripe webhook signature verification completes under Bun using constructEventAsync (sync constructEvent is proven to fail)"
    - "A tampered Stripe signature is REJECTED under Bun (verification is real, not disabled)"
    - "@rdkit/rdkit WASM loads under Bun and parses a molecule via a standalone script (not via an app route)"
    - "The required Phase 5 prod sync->async webhook migration is captured in a committed handoff file"
  artifacts:
    - path: "spike/04-stripe.ts"
      provides: "Stripe constructEventAsync positive + negative verification under Bun"
      contains: "constructEventAsync"
    - path: "spike/05-rdkit.ts"
      provides: "RDKit WASM load + get_mol under Bun"
      contains: "initRDKitModule"
    - path: "spike/STRIPE-HANDOFF.md"
      provides: "Phase 5 handoff documenting the prod server/index.js:99 sync->async fix"
      contains: "constructEventAsync"
  key_links:
    - from: "spike/04-stripe.ts"
      to: "stripe SDK"
      via: "webhooks.constructEventAsync"
      pattern: "constructEventAsync"
---

<objective>
Prove the two Docker-free Bun compatibility findings from RESEARCH.md with standalone throwaway spike scripts: Stripe webhook signature verification (CMPT-04) using the async variant that works under Bun, and @rdkit/rdkit WASM (CMPT-05). Both run without any live service, so this plan needs no Docker and binds no ports — it can run in parallel with the live-service plan.

Purpose: CMPT-04 is the headline RESEARCH finding — the prod sync `constructEvent()` at server/index.js:99 THROWS under Bun; this plan proves `constructEventAsync()` is the answer AND that verification is genuinely enforced (negative test), then records the required Phase 5 prod fix so it is not lost. CMPT-05 proves a dep that is installed but imported nowhere in the app.

Output: spike/04-stripe.ts, spike/05-rdkit.ts, spike/STRIPE-HANDOFF.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stripe webhook verification spike (CMPT-04) — positive + negative</name>
  <files>spike/04-stripe.ts</files>
  <read_first>
    - .planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md (Pitfall 1, the "Stripe webhook verification that WORKS under Bun" code example, and the Security Domain section's negative-test requirement)
    - server/index.js lines 99-108 (the prod webhook handler using the sync constructEvent that must NOT be edited)
  </read_first>
  <action>Create spike/04-stripe.ts as a standalone Bun script. Import Stripe from "stripe" and crypto from "node:crypto". Read STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET from process.env (never hardcode; a test/dummy STRIPE_WEBHOOK_SECRET value is acceptable since constructEventAsync only HMACs against it). Build a signed Stripe-style header: ts = current unix seconds, sig = HMAC-SHA256 over `${ts}.${payload}` with the webhook secret, header = `t=${ts},v1=${sig}`. POSITIVE case: call `await stripe.webhooks.constructEventAsync(payload, header, secret)` and print the parsed event.type. NEGATIVE case (MANDATORY per Security Domain): mutate the signature (flip a hex char) or the payload, call constructEventAsync again inside a try/catch, and assert it THROWS — print "tampered payload correctly rejected". The script must exit 0 only if the valid sig verifies AND the tampered sig is rejected; exit 1 otherwise. Do NOT use the sync constructEvent (RESEARCH proves it throws under Bun).</action>
  <verify>
    <automated>bun run spike/04-stripe.ts</automated>
  </verify>
  <acceptance_criteria>`bun run spike/04-stripe.ts` prints the parsed event type for the valid signature, prints "tampered payload correctly rejected" for the mutated signature, and exits 0. The script source contains constructEventAsync and contains NO call to the sync `constructEvent(`. Grep confirms no hardcoded sk_live/sk_test secret literal in the file.</acceptance_criteria>
  <done>CMPT-04 proven under Bun: a valid webhook signature verifies via constructEventAsync and a tampered one is rejected; exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: RDKit WASM spike (CMPT-05)</name>
  <files>spike/05-rdkit.ts</files>
  <read_first>
    - .planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md (Pitfall 2 and the "RDKit WASM load + real op" code example — VERIFIED working under Bun 1.3.14)
    - server/package.json (confirms @rdkit/rdkit ^2025.3.4-1.0.0 is a server dependency; the WASM dist lives at server/node_modules/@rdkit/rdkit/dist/RDKit_minimal.js)
  </read_first>
  <action>Create spike/05-rdkit.ts as a standalone Bun script. Import initRDKitModule from the installed dist path (server/node_modules/@rdkit/rdkit/dist/RDKit_minimal.js — run the script from repo root or resolve relative to it). Call `await initRDKitModule()`, then `RDKit.get_mol("c1ccccc1")` (benzene), assert `mol.is_valid()` is true, print RDKit.version() and the canonical SMILES via mol.get_smiles(), then call mol.delete(). Exit 0 on a valid molecule, exit 1 otherwise. This proves CMPT-05 standalone — RESEARCH confirms @rdkit/rdkit is imported NOWHERE in app code, so it must not be proven via a route.</action>
  <verify>
    <automated>bun run spike/05-rdkit.ts</automated>
  </verify>
  <acceptance_criteria>`bun run spike/05-rdkit.ts` prints an RDKit version string (e.g. 2025.03.x) and the canonical SMILES for benzene, with mol.is_valid() true, and exits 0. Script contains initRDKitModule and get_mol.</acceptance_criteria>
  <done>CMPT-05 proven: RDKit WASM initializes and parses benzene under Bun via a standalone script; exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Write Phase 5 Stripe sync->async handoff (CMPT-04 deliverable)</name>
  <files>spike/STRIPE-HANDOFF.md</files>
  <read_first>
    - .planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md (Pitfall 1, State of the Art row on createNodeCryptoProvider, and the headline finding)
    - server/index.js lines 99-108 (exact prod call site to migrate)
    - server/test/stripe-webhook.test.mjs (the bespoke test that also uses the sync form and will break under Bun until changed)
  </read_first>
  <action>Create spike/STRIPE-HANDOFF.md documenting the REQUIRED Phase 5 (RUN-03 territory) production change, surfaced now so it is not lost: (1) server/index.js:99 must change `stripe.webhooks.constructEvent(...)` to `await stripe.webhooks.constructEventAsync(...)` inside the already-async webhook handler; (2) server/test/stripe-webhook.test.mjs uses the sync form and must be updated identically; (3) note that `createNodeCryptoProvider()` is NOT an escape hatch — it errors under Bun; (4) include the exact error the sync path produces under Bun ("SubtleCryptoProvider cannot be used in a synchronous context") so Phase 5 recognizes it. State explicitly that NO prod code is edited in Phase 4 — this is the spike surfacing the fix, not applying it. Reference the verified positive+negative proof in spike/04-stripe.ts.</action>
  <verify>
    <automated>test -f spike/STRIPE-HANDOFF.md && grep -q constructEventAsync spike/STRIPE-HANDOFF.md && grep -q "server/index.js" spike/STRIPE-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>spike/STRIPE-HANDOFF.md exists and references constructEventAsync, the server/index.js:99 call site, server/test/stripe-webhook.test.mjs, the createNodeCryptoProvider non-availability, and states no prod code is edited in Phase 4. No real secret literals committed.</acceptance_criteria>
  <done>The Phase 5 prod sync->async migration is captured in a committed, durable handoff file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike scripts -> env/secrets | Spike reads STRIPE_SECRET_KEY/STRIPE_WEBHOOK_SECRET; must not hardcode or commit real keys |
| spike dir -> repo | Spike is throwaway/isolated; must not become a shipped, unauthenticated entry point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Stripe webhook verify (spike/04-stripe.ts) | mitigate | Negative test asserts a tampered signature is REJECTED, not only that a valid one passes (per RESEARCH Security Domain) |
| T-04-02 | Info Disclosure | Secrets in committed spike files | mitigate | Secrets read from process.env only; grep gate confirms no sk_*/secret literals in spike scripts or handoff |
| T-04-03 | Elevation of Privilege | spike dir as shipped entry point | accept | Scripts are standalone CLI files run via `bun run`, never mounted into the Express app or Docker prod image; throwaway per RESEARCH |
</threat_model>

<verification>
Run both spike scripts under Bun from repo root; both exit 0. Confirm STRIPE-HANDOFF.md exists with required references. Confirm no hardcoded secrets via grep.
</verification>

<success_criteria>
- CMPT-04: constructEventAsync verifies a valid webhook and rejects a tampered one under Bun (exit 0)
- CMPT-05: RDKit WASM loads and parses benzene under Bun (exit 0)
- Phase 5 sync->async handoff committed
- No secrets committed
</success_criteria>

<output>
Create `.planning/phases/04-compatibility-spike-baseline/04-01-SUMMARY.md` when done
</output>
