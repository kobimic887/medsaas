---
phase: 04-compatibility-spike-baseline
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - spike/01-boot-health.ts
  - spike/Dockerfile.bun
  - spike/README.md
autonomous: true
requirements: [CMPT-01, CMPT-06]
must_haves:
  truths:
    - "The UNMODIFIED Express server boots under Bun on arm64 and GET /health returns the real app health body"
    - "The oven/bun:1.3.14-slim arm64 image builds and the running container serves /health with 200"
    - "A spike README documents how to run every spike script and what each proves"
  artifacts:
    - path: "spike/01-boot-health.ts"
      provides: "Boots server/index.js under Bun, polls /health, asserts 200"
      contains: "/health"
    - path: "spike/Dockerfile.bun"
      provides: "oven/bun:1.3.14-slim image building + running the server (CMPT-06)"
      contains: "oven/bun:1.3.14-slim"
    - path: "spike/README.md"
      provides: "Run instructions + results table for all spike scripts"
  key_links:
    - from: "spike/01-boot-health.ts"
      to: "server/index.js"
      via: "spawn under Bun + poll /health"
      pattern: "/health"
    - from: "spike/Dockerfile.bun"
      to: "oven/bun base image"
      via: "FROM oven/bun:1.3.14-slim"
      pattern: "oven/bun:1.3.14-slim"
---

<objective>
Prove the Express server boots under Bun (CMPT-01) and inside the oven/bun arm64 container (CMPT-06), serving the existing /health endpoint. Both tasks bind port 3000, so they live in Wave 2 (after the live-service plan brings up Mongo, which the server connects to at boot) and must not run concurrently with the Wave 3 baseline plan that also binds :3000. Also write the spike README.

Purpose: CMPT-01 boot is NOT zero-dependency (RESEARCH Pitfall 4) — server/index.js validates MONGODB_URI, JWT_SECRET >=32 chars, STRIPE_SECRET_KEY and connects to Mongo before serving, so this plan depends on plan 02's live Mongo. The spike must boot the UNMODIFIED app (no edits to server/index.js — the Stripe sync->async fix is Phase 5). CMPT-06 proves the prod deployment unit (oven/bun image, version-pinned) on arm64.

Output: spike/01-boot-health.ts, spike/Dockerfile.bun, spike/README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Express boot + /health under Bun (CMPT-01)</name>
  <files>spike/01-boot-health.ts</files>
  <read_first>
    - .planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md (Pitfall 4 — boot validates env + connects Mongo first; CMPT-01 and CMPT-02 share preconditions)
    - server/index.js lines 289-292 (the real /health route returns {status:'OK', timestamp, message}) and lines 4710, 4740 (PORT default 3000, app.listen on 0.0.0.0)
    - CLAUDE.md (required startup env vars: MONGODB_URI, JWT_SECRET >=32 chars, STRIPE_SECRET_KEY)
  </read_first>
  <action>Create spike/01-boot-health.ts as a standalone Bun script that spawns the UNMODIFIED server under Bun (run `bun server/index.js` from repo root via Bun.spawn) with valid env vars supplied from the existing root .env (MONGODB_URI pointing at the live local Mongo from plan 02, JWT_SECRET >=32 chars, STRIPE_SECRET_KEY). Use PORT=3000. Poll GET http://localhost:3000/health every ~250ms until it returns 200, with a startup timeout of ~30s. Assert the response status is 200 and the JSON body has status equal to the string OK (the ACTUAL app body — capital OK, not lowercase). Print PASS and the body, then kill the spawned server process. Exit 0 only on a 200 with the expected body. Do NOT edit server/index.js.</action>
  <verify>
    <automated>bun run spike/01-boot-health.ts</automated>
  </verify>
  <acceptance_criteria>`bun run spike/01-boot-health.ts` spawns the unmodified server under Bun, GET /health returns 200 with JSON body status equal to OK, the script prints PASS and exits 0, and the spawned server is terminated afterward. server/index.js is byte-for-byte unchanged (git diff shows no modification).</acceptance_criteria>
  <done>CMPT-01 proven: the unmodified Express server boots under Bun and /health returns 200 with the real body; exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: oven/bun arm64 container build + serve /health (CMPT-06)</name>
  <files>spike/Dockerfile.bun</files>
  <read_first>
    - .planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md (Standard Stack oven/bun variants — use 1.3.14-slim, version-pinned, glibc for WASM safety; Anti-Pattern: no floating tags)
    - Dockerfile (the existing node:22-alpine prod image structure to adapt: server deps, FRONTEND_DIST, EXPOSE 3000, CMD)
  </read_first>
  <action>Create spike/Dockerfile.bun using FROM oven/bun:1.3.14-slim (exact pinned tag, NOT latest/floating — RESEARCH credibility requirement). Build a minimal image that installs server deps and runs the server under Bun: copy server/, install with `bun install`, set required env so /health is reachable. The server connects to Mongo at boot, so run the container against a reachable Mongo — either host networking pointing MONGODB_URI at the host Mongo, or the same docker network as the spike Mongo; document the chosen approach in the README. EXPOSE 3000, CMD running the server under bun. Target arm64 (host + prod VPS are arm64). Build with `docker build -f spike/Dockerfile.bun -t medsaas:bun-spike .`, run the container mapping :3000, and curl /health expecting 200. After verification the image may be pruned; do not push to any registry (throwaway per RESEARCH).</action>
  <verify>
    <automated>docker build -f spike/Dockerfile.bun -t medsaas:bun-spike . && docker image inspect medsaas:bun-spike --format '{{.Architecture}}' | grep -q arm64</automated>
    <human-check>Run the medsaas:bun-spike container with a reachable Mongo and confirm curl http://localhost:3000/health returns HTTP 200, per the README run instructions.</human-check>
  </verify>
  <acceptance_criteria>`docker build -f spike/Dockerfile.bun -t medsaas:bun-spike .` succeeds and `docker image inspect` reports Architecture arm64. Running the container against a reachable Mongo, curl http://localhost:3000/health returns HTTP 200. spike/Dockerfile.bun contains the pinned tag oven/bun:1.3.14-slim and no floating tag.</acceptance_criteria>
  <done>CMPT-06 proven: the version-pinned oven/bun arm64 image builds and the running container serves /health with 200.</done>
</task>

<task type="auto">
  <name>Task 3: Spike README with run instructions + results table</name>
  <files>spike/README.md</files>
  <read_first>
    - .planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md (Recommended Project Structure and the "data flow to trace" — what each script proves)
    - spike/04-stripe.ts, spike/05-rdkit.ts, spike/02-mongo.ts, spike/03-amqp.ts, spike/01-boot-health.ts, spike/Dockerfile.bun (the scripts to document)
  </read_first>
  <action>Create spike/README.md documenting: (1) prerequisites (Bun 1.3.14, Docker up, `npm run services:up`, valid root .env); (2) one row per spike script mapping it to its CMPT requirement, the exact run command (`bun run spike/0N.ts`, and the docker build/run for Dockerfile.bun), and what real operation it proves; (3) a results table to fill in PASS/FAIL with notes (especially the amqplib #5627 watch and the Stripe negative test); (4) a note that spike/ is throwaway — scripts deleted after Phase 5, but BASELINE.md and STRIPE-HANDOFF.md are committed deliverables; (5) the exact container run command + Mongo-reachability approach used for CMPT-06. No secrets in the README.</action>
  <verify>
    <automated>test -f spike/README.md && grep -q "bun run spike/01-boot-health.ts" spike/README.md && grep -q "CMPT-0" spike/README.md</automated>
  </verify>
  <acceptance_criteria>spike/README.md exists, lists each spike script with its CMPT id and exact run command, includes the container build/run command for CMPT-06, includes a results table, and notes spike throwaway status. No secrets committed.</acceptance_criteria>
  <done>A reviewer can run every spike from the README without reading source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike server boot -> env/secrets | Server reads MONGODB_URI/JWT_SECRET/STRIPE_SECRET_KEY from env; spike supplies them from .env, never hardcoded |
| spike container -> registry | Throwaway image must not be pushed to any registry or become a shipped artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Info Disclosure | Secrets baked into spike Dockerfile/README | mitigate | Dockerfile.bun and README take env at run time (--env-file/-e), never bake or print secrets; grep gate confirms no secret literals |
| T-04-08 | Elevation of Privilege | Spike container as shipped entry point | accept | Image tagged medsaas:bun-spike, never pushed, pruned after verify; prod image is Phase 7 (OPS-01) |
| T-04-09 | Tampering | Accidental edit to server/index.js during boot spike | mitigate | CMPT-01 acceptance asserts `git diff` shows server/index.js unchanged — boot proves the UNMODIFIED app |
</threat_model>

<verification>
Run spike/01-boot-health.ts under Bun (server unchanged, /health 200). Build the oven/bun arm64 image and confirm the container serves /health 200. README documents all scripts. No secrets committed.
</verification>

<success_criteria>
- CMPT-01: unmodified server boots under Bun, /health returns 200 with body status OK (exit 0)
- CMPT-06: pinned oven/bun:1.3.14-slim arm64 image builds and container serves /health 200
- README documents every spike script and the container run
- server/index.js unchanged; no secrets committed
</success_criteria>

<output>
Create `.planning/phases/04-compatibility-spike-baseline/04-03-SUMMARY.md` when done
</output>
