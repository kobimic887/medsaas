---
phase: 04-compatibility-spike-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - spike/02-mongo.ts
  - spike/03-amqp.ts
autonomous: true
requirements: [CMPT-02, CMPT-03]
must_haves:
  truths:
    - "The MongoDB driver connects, inserts, finds, and creates an index against a live Mongo under Bun"
    - "amqplib publishes a message and consumes the same payload back against a live RabbitMQ under Bun (live publish/consume round-trip)"
    - "If amqplib produces an 'Invalid frame' error (#5627), it is captured as a finding rather than failing silently"
  artifacts:
    - path: "spike/02-mongo.ts"
      provides: "Mongo connect/insert/find/createIndex under Bun"
      contains: "createIndex"
    - path: "spike/03-amqp.ts"
      provides: "amqplib publish+consume round-trip under Bun"
      contains: "consume"
  key_links:
    - from: "spike/02-mongo.ts"
      to: "live MongoDB :27017"
      via: "MongoClient.connect"
      pattern: "createIndex"
    - from: "spike/03-amqp.ts"
      to: "live RabbitMQ :5672"
      via: "amqp.connect -> publish -> consume"
      pattern: "consume"
---

<objective>
Prove the two live-service Bun compatibility findings against running local services (MongoDB + RabbitMQ via `npm run services:up`): MongoDB driver (CMPT-02) and amqplib (CMPT-03). These are CLIENT scripts only — they bind no HTTP port — so they run in parallel with plan 01.

Purpose: amqplib (CMPT-03) is the SINGLE highest-risk task in the phase — RESEARCH could not pre-verify the live publish/consume because Docker was down, and bug #5627 ("Invalid frame") is still open. Run it first. CMPT-02 (Mongo) is the lowest-risk dep but its createIndex path mirrors the server startup index creation, so it must be exercised, not imported.

Output: spike/02-mongo.ts, spike/03-amqp.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md
@server/utils/rabbitMQUtils.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start live services (precondition for CMPT-02/03)</name>
  <files>(no files — runtime precondition)</files>
  <read_first>
    - .planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md (Environment Availability table and Open Question 1 — Docker must be up; RabbitMQ has no fallback)
    - CLAUDE.md (services:up = `docker compose up -d mongo rabbitmq`)
  </read_first>
  <action>Run `npm run services:up` (which executes `docker compose up -d mongo rabbitmq`) to bring up MongoDB on :27017 and RabbitMQ on :5672. Then poll until both are reachable (e.g. `docker compose ps` shows mongo and rabbitmq running/healthy; RabbitMQ takes ~10-20s to accept AMQP connections after container start). If the Docker daemon is not running, this surfaces as a runtime gate — start Docker Desktop and retry; do NOT treat this as a manual checkpoint task. Read MONGODB_URI and RABBITMQ_URL from the existing root .env (reuse existing env var names per RESEARCH Runtime State Inventory).</action>
  <verify>
    <automated>docker compose ps mongo rabbitmq | grep -E "running|Up|healthy"</automated>
  </verify>
  <acceptance_criteria>`docker compose ps` shows both mongo and rabbitmq containers Up. RabbitMQ accepts AMQP connections (a subsequent connect in Task 3 succeeds).</acceptance_criteria>
  <done>MongoDB :27017 and RabbitMQ :5672 are running locally and reachable.</done>
</task>

<task type="auto">
  <name>Task 2: MongoDB driver spike (CMPT-02)</name>
  <files>spike/02-mongo.ts</files>
  <read_first>
    - .planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md (Pattern 1 MongoDB example; the createIndex call mirrors server/index.js:715-723)
    - server/index.js lines 710-723 (the real startup connect + createIndex path being mirrored)
  </read_first>
  <action>Create spike/02-mongo.ts as a standalone Bun script. Import MongoClient from "mongodb". Read MONGODB_URI from process.env (reuse existing name). Connect, select a THROWAWAY test database (e.g. db "bun_spike_test" — never a prod db), insertOne a small doc, findOne it back and assert equality, then createIndex on the test collection (mirrors the prod startup index path). Print the inserted _id. Drop the test database at the end (cleanup per RESEARCH Runtime State Inventory). Close the client. Exit 0 only if connect+insert+find+createIndex all succeed.</action>
  <verify>
    <automated>bun run spike/02-mongo.ts</automated>
  </verify>
  <acceptance_criteria>`bun run spike/02-mongo.ts` connects, inserts one doc, finds it back, creates an index, prints the inserted _id, drops the throwaway db, and exits 0. Script contains createIndex. Uses a throwaway db name, not a prod collection.</acceptance_criteria>
  <done>CMPT-02 proven: Mongo driver connect/insert/find/createIndex works under Bun against live Mongo; exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: amqplib publish+consume spike (CMPT-03) — HIGHEST RISK, run first</name>
  <files>spike/03-amqp.ts</files>
  <read_first>
    - .planning/phases/04-compatibility-spike-baseline/04-RESEARCH.md (Pitfall 3 + Open Question 1 + Assumption A2 — this is the one CMPT dep whose live op is UNVERIFIED; #5627 "Invalid frame" is a capturable finding)
    - server/utils/rabbitMQUtils.js (the real connect -> createChannel -> assertQueue -> publish/consume pattern to mirror)
  </read_first>
  <action>Create spike/03-amqp.ts as a standalone Bun script mirroring server/utils/rabbitMQUtils.js. Import amqp from "amqplib". Read RABBITMQ_URL from process.env (reuse existing name; default amqp://guest:guest@localhost:5672 if unset). connect -> createChannel -> assertQueue(a throwaway queue name e.g. "bun_spike_queue", {durable:true}) -> sendToQueue/publish a known payload (e.g. JSON {ping: Date.now()}) -> consume the same queue and assert the consumed message body equals the published payload, then ack. Print the round-tripped payload. Use a timeout (e.g. 10s) so a hang fails rather than blocks forever. Wrap connect/consume in try/catch: if an "Invalid frame" error occurs (bug #5627), print it explicitly as a CAPTURED FINDING and exit non-zero so it is surfaced — do not swallow it. Delete the throwaway queue and close the connection on success. Exit 0 only on a verified round-trip.</action>
  <verify>
    <automated>bun run spike/03-amqp.ts</automated>
  </verify>
  <acceptance_criteria>`bun run spike/03-amqp.ts` publishes a payload and the consumer prints the identical payload, then exits 0. If an "Invalid frame" error appears, it is printed as a captured finding (not swallowed) and the script exits non-zero. Script contains consume and uses a throwaway queue.</acceptance_criteria>
  <done>CMPT-03 resolved: a live amqplib publish->consume round-trips under Bun (exit 0), OR the #5627 "Invalid frame" failure is captured as an explicit finding for follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike scripts -> live Mongo/RabbitMQ | Spike consumes local services but writes only to throwaway db/queue; alters no prod config |
| spike scripts -> env/secrets | MONGODB_URI / RABBITMQ_URL read from env, never hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | Spike writes to live Mongo (spike/02-mongo.ts) | mitigate | Writes only to a throwaway "bun_spike_test" db that is dropped at end; never touches prod collections |
| T-04-05 | Info Disclosure | Connection URIs in committed spike files | mitigate | MONGODB_URI/RABBITMQ_URL read from process.env; grep gate confirms no credential literals committed |
| T-04-06 | Denial of Service | amqp consumer hang under #5627 | accept | Script uses a bounded timeout and captures Invalid-frame as a finding; spike is local/throwaway only |
</threat_model>

<verification>
Bring up services, then run spike/02-mongo.ts and spike/03-amqp.ts under Bun from repo root. Mongo exits 0; amqp either exits 0 (round-trip) or surfaces a captured Invalid-frame finding. No credential literals committed.
</verification>

<success_criteria>
- CMPT-02: Mongo connect/insert/find/createIndex under Bun (exit 0)
- CMPT-03: amqplib live publish->consume round-trip under Bun (exit 0) or captured #5627 finding
- Writes confined to throwaway db/queue; no secrets committed
</success_criteria>

<output>
Create `.planning/phases/04-compatibility-spike-baseline/04-02-SUMMARY.md` when done
</output>
