# AGENTS.md

**This file is a pointer. The guidance lives in [`CLAUDE.md`](./CLAUDE.md) — read that.**

`AGENTS.md` previously held a near byte-for-byte copy of `CLAUDE.md`, including its
`# CLAUDE.md` heading. The copy drifted, and both files told agents to use a planning
workflow whose skills are not installed. One file, one source of truth: everything stays in
`CLAUDE.md`, and this remains a redirect so agents that look for `AGENTS.md` by convention
still find their way.

Longer-form references live in [`docs/`](./docs/README.md).
